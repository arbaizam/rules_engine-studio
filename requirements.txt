streamlit>=1.62
pandas>=2.0
pyyaml>=6.0
# Keep Community Cloud installs on Spark's smaller 3.5 maintenance line.
# The engine supports 3.5+, while unconstrained resolution now selects the
# substantially larger PySpark 4.2 source distribution during every cold build.
pyspark==3.5.9
rules-engine @ git+https://github.com/arbaizam/rules_engine.git@667f80d5fa9e660687268d9752b53fbaced2e8f1
# Optional, for reading .parquet sample data:
# pyarrow>=14.0
